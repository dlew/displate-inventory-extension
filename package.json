{
  "name": "displate-inventory-extension",
  "version": "2.0.0",
  "scripts": {
    "watch": "webpack --watch",
    "build": "webpack",
    "zip": "npm run build && rm -rf output && mkdir -p output && zip -r output/extension.zip dist/"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.3",
    "webpack": "^5.92.1",
    "webpack-cli": "^5.1.4"
  }
}
